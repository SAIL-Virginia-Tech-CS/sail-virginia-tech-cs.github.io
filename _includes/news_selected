<div class="news-items">
    <ul>
        {% for item in site.data.news %}
                {% if item.selected %}
                        <li>({{ item.date }}) {{ item.title }}</li>
                {% endif %}
        {% endfor %}
    </ul>
</div>
